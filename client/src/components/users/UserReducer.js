const user = (state, action) => {
  switch(action.type) {
    default:
      return state
  }
}

const reducer = (state = [], action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default reducer
